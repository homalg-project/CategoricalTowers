#
# M2: Intrinsic modules with elements for the CAP based homalg
#
# Implementations
#
InstallGlobalFunction( M2_Example,
function()
	Print( "This is a placeholder function, replace it with your own code.\n" );
end );

